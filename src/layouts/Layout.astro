---
import MetaTags from '@/components/MetaTags.astro';
import Header from '@/components/layout/Header.tsx';
import CookieBanner from '@/components/CookieBanner.tsx';
import Footer from '@/components/layout/Footer.astro';
import WhastappShortcut from '@/components/WhastappShortcut.astro';
import '@/styles/global.css';

interface Props {
  title: string;
  description: string;
  schema?: object;
}

const { title, description, schema } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <MetaTags title={title} description={description} schema={schema} />
  </head>
  <body class="font-display">
    <a
      href="#main"
      class="sr-only mt-20 focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:rounded-lg focus:bg-primary focus:px-4 focus:py-2 focus:text-white focus:outline-none"
    >
      Saltar al contenido
    </a>
    <Header client:load />
    <main
      id="main"
      class="bg-neutral-950 bg-[radial-gradient(ellipse_80%_80%_at_50%_-20%,rgba(120,119,198,0.3),rgba(255,255,255,0))]"
    >
      <slot />
    </main>
    <CookieBanner client:load />
    <WhastappShortcut />
    <Footer />
  </body>
</html>
